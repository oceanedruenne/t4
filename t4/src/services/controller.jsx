import person from "../data/person.json"
import steps from "../data/steps.json"
import jauge from "../routes/jauge"


export default function Controller()
{
    // Faire en sorte de récupérer le tableau des options pour toutes les questions que l'on peut poser 
    let first_question = document.getElementById("first_issues");
    console.log(first_question);
    // Récupérer les valeurs de chaque pensées
    // Faire les opérations correspondantes
}